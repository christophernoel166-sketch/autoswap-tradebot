/**
 * ===================================================
 * Redis command channels (API → BOT)
 * ===================================================
 */

export function manualSellCommandKey() {
  return "cmd:manual-sell";
}
